# SWARM_OPERATIONS.md — Roles, Boundaries, Timeline
**Companion to SWARM_MASTER.md (the constitution). This file governs HOW we work. Every chat reads both before acting.**
**Status: ACTIVE. Amendments only via the Master chat.**

---

## 1. The End State (Definition of Done — December)

> **Three interceptor drones autonomously form and hold a moving ring around Samrat's FPV quad, with the Eye airborne overhead as the fleet's fourth airframe, while a teammate flies the FPV quad, outdoors, with all failsafes armed and the mission logged and scored.**

That single sentence is the goal (originally dated October, revised to December by §4's slippage — see below). Read it carefully — it also defines what this milestone does **NOT** require:
- **No camera-based detection by this milestone.** The target feed is still the ₹1,200 GPS beacon taped to the FPV quad (its approved development role per SWARM_MASTER §5). The Eye flies from P3 onward as the fourth airframe (A20/A21) — but its role there is airborne positioning and, on the UAV-X software track, relay eligibility (A21) — not detection. Camera-based detection replaces the beacon **after** this milestone, before Techkriti. The swarm doesn't know or care where the target position comes from — that was the whole point of the layered design.
- No demo polish, no improvement graph, no audience screen. Those come in the Techkriti phase.

A chat pulling work forward from the Techkriti phase into this phase is scope creep. Flag it.

**Fleet size note:** this section originally said "five interceptor drones." A20 (22 Sep 2026) finalized the real fleet at 3 interceptors + 1 Eye, 4 airframes total, superseding that figure everywhere it appeared in this file and in SWARM_MASTER §2/§3. See A20 in the Amendment Log for the full reasoning, including what does and doesn't carry over from evidence already collected at 5-interceptor scale.

**Containment-claim note (A22):** "ring"/"surrounds the target" is a static-geometry claim and holds at 3 interceptors exactly as it did at 5. The dynamic claim — no exploitable gap against an actively evading target — is measurably weaker at 3. See A22 for the derivation; it governs paper language, not the demo itself.

**Ring-accuracy scope note (A24):** for Stage 1, the paper presents the ring as static-geometry/qualitative (per A22) plus the Sprint 3 RTK-grade plumbing evidence, explicitly labelled as such. No quantitative ring-accuracy figure under degraded GPS/wind conditions (A12) is required or expected for Stage 1 — see A24 for why.

## 2. Role Charters — what each chat does, and where it STOPS

Every chat is a specialist, not a general. The test for any task: *"Is this mine?"* If there is any doubt, it is not yours — flag it to Master in one line and stop.

### Master chat
- **Does:** rules on conflicts between chats; owns Open Questions; amends SWARM_MASTER and this file; makes trade-off decisions (budget, scope, schedule); reviews phase gates.
- **Does NOT:** write code, pick parts, run sim sessions. If Master starts doing a sub-chat's homework, the structure has failed.

### Hardware chat (B1)
- **Does:** exact parts, India pricing/stock, weight budgets, build sheets, wiring plans, sourcing alternatives when items go out of stock.
- **Does NOT:** change the architecture (frame material, radio bands, sensor strategy — those are Master rulings, even when hardware data motivates them); does not opine on software or sim; does not adjust budget totals by silently swapping quality tiers — cost changes are flagged, not absorbed.
- **Unpaused by A20 (22 Sep 2026).** A3's budget question is closed — the fleet is finalized at 3 interceptors + 1 Eye (4 airframes), which resolves the budget gap by headcount rather than by funding it or downgrading parts. B1 may resume parts lists and build sheets for the finalized fleet.

### Software chat (B2)
- **Does:** designs the commander, geometry, state machine, detection pipeline; writes every Claude Code / Cowork prompt; explains the concepts so Samrat can rederive them; maintains CLAUDE.md.
- **Does NOT:** run sessions or debug live sim output (that's Simulation's lane); does not choose hardware; does not relax any safety rule in CLAUDE.md — ever, for anyone.

### Simulation chat (B3)
- **Does:** turns module prompts into session plans (checkpoints, "done when" criteria, data to capture); judges Cowork's results, logs, and tuning notes; declares modules passed or failed.
- **Does NOT:** execute work (that is Cowork's lane now), redesign modules (send it back to B2), change safety parameters to make a test pass (flag to Master), or declare a module done without the module's "done when" evidence.

### Cowork (the Worker — executes for ALL chats)
- **Does:** all hands-on execution for every chat: edits code, runs SITL, reads output, produces logs and evidence. Thinking chats (Master/B1/B2/B3) generate prompts and judgments; Cowork does the work. Any chat needing something *done* writes a Cowork prompt; Samrat carries it.
- **Does NOT:** make design decisions, deviate from the prompt it was given, or touch anything named failsafe, geofence, RTL, or arm beyond what its prompt specifies. If a prompt is ambiguous, it asks instead of improvising. **This explicitly includes amending this file — amendments happen only via the Master chat (see file header). Cowork reports findings back for Master/the owning chat to log, it does not write ruling entries itself (see A30's own postscript, added 25 Sep, for why this line was strengthened).**

### Helper chat ("idiot-proofer")
- **Does:** quick foundational questions (what a command does, how to open Ubuntu, what a term means). Cheap, fast, no project decisions.
- **Does NOT:** make any decision that other chats would record. If the answer would change a file in the repo or a line in a project doc, it belongs in a real chat.

### Escalation rules (unchanged, restated)
1. Any cross-domain question → Master, in one line, immediately — don't solve it "just this once."
2. Same bug, 3 failed attempts → escalate the model (Sonnet 5 → Opus 4.8) or the chat (sub-chat → Master).
3. Anything touching failsafes, arming, geofence → never weakened, only escalated.
4. **Disagreement between two sub-chats (added 23 Sep, per A22 episode):** neither chat can see the other. A "B2 and B3 disagree" situation is not evidence of a bug — it's the direct, designed consequence of §3 below. Samrat pastes both positions to Master; Master rules; Samrat carries the ruling back. Do not try to get B2 and B3 to "resolve it between themselves" — they structurally cannot.

## 3. The Working Loop (who prompts whom)

```
Samrat decides intent (with Master)
   → thinking chat produces a Cowork prompt
     (B2 for code/modules, B1 for hardware tasks, B3 for session plans)
      → Cowork EXECUTES (code, SITL runs, logs, evidence)
      → B3 judges sim results; the owning chat judges everything else
   → Master reviews at phase gates only
```
**Amendment A5:** Cowork is the single executor for the whole project. Thinking chats think; Cowork does. In time-boxed sprints, Master may hand Cowork a self-contained sprint prompt directly, provided every safety rule in CLAUDE.md travels inside the prompt and B3 still judges the evidence afterward.

Samrat is the only carrier of information between chats. No chat should assume another chat saw anything unless Samrat pasted it. **This is deliberate, not an accident of tooling** — it's what makes A9's evidence discipline enforceable (nothing gets "confirmed" by one chat's unverified say-so reaching another). With days left to 27 Sep, the overhead of this is real; A5's direct-to-Cowork shortcut exists precisely for this crunch period — use it for well-specified remaining tasks rather than routing everything through the full B2→B3→Cowork chain.

## 4. Timeline (REVISED 5 September 2026; fleet counts REVISED 23 September 2026 per A20)

P1 slipped ~3 weeks. Per the slippage rule, everything shifts and no gate is skipped. Revised phases:

| Phase | Dates | Work | GATE to pass |
|---|---|---|---|
| **P1 — Sim complete** | now → 26 Sep | Modules 3–5 in SITL. A3 budget ruling CLOSED by A20. | Full mission in sim: **3 interceptors** ring a MOVING target. **Quantitative accuracy under A12-degraded GPS/wind is NOT required for Stage 1 — see A24.** Every failsafe tested. **Only then: order interceptor #1.** |
| **P2 — First real drone** | 27 Sep → 25 Oct | Build interceptor #1, bench tests (props off), kill-switch drills, real Module 2 on dry days. **A7 confirmed before first flight (A10).** The A12 degraded-GPS/wind baseline (deferred from Stage 1 per A24) belongs here, against real hardware GPS, not simulated. | One real drone repeats its sim behavior. **Only then: order the other 3 (2 interceptors + the Eye).** |
| **P3 — Real swarm** | 26 Oct → 30 Nov | Build the remaining 3 airframes (2 interceptors + Eye), real static ring, then moving ring (beacon on a person/RC car first). | **3 real interceptors** hold a moving ring; the Eye flies overhead (not part of the ring geometry). |
| **P4 — END STATE** | Dec | Beacon on the FPV quad, teammate flies it, swarm rings it. Learning loop scores every mission. | **The §1 sentence, on video, with logs.** |
| **P5 — Techkriti phase** | Jan → fest | Eye refit, detection pipeline (trained on P3/P4 footage — record everything now), beacon→camera swap, improvement graph, rehearsals. | Demo-ready. |

**The October end state in §1 is now a December end state.** Stated plainly rather than quietly: the sentence is unchanged in substance (only the fleet count changed, via A20), the date moved. Spring-break build time lands inside P2/P3, which is where hands-on work belongs.

**Slippage rule (unchanged):** dates may slip; **gate order may not.** A late, working swarm beats an on-time crash compilation.

**Standing risks:** SpeedyBee stack stock (Hardware watches, doesn't buy in excess), flight windows, school workload. Master owns re-planning if any of these bite.

## 4a. Sprint Plan — Weeks 1–3 (5 → 26 September)

Three weeks to the P1 gate. One module per week, plus the viewing capability Samrat has asked for.

**Note on fleet size in this section:** everything below is a historical record of what was actually run and judged during Weeks 1–2, at the fleet size that was current at the time (5 interceptors + 1 simulated target, per A6). It is left as-is rather than rewritten to say "3" — the runs happened, the evidence is real, and rewriting a log to match a later decision is exactly what A9's evidence discipline exists to prevent. A20 (in the Amendment Log) explains what this evidence does and doesn't prove for the now-finalized 3-interceptor fleet. Week 3 and anything not yet run should target the current fleet (3 interceptors + Eye) going forward.

### Week 1 (5 – 12 Sep) — Close Module 3, get eyes on the ring
**Goal: Module 3 stamped, and the ring visible live in Mission Planner.**
1. **A7 diagnosis (Cowork, standalone):** second independent client reads `SYSID_MYGCS` on one vehicle. Reports the raw number only. This decides whether A7 is "echo broken" or "never applied."
2. **B2:** audit the param-set path in `ring.py` against the pump/drain race; identify SITL parameters governing GPS accuracy class and wind for the A12 profile.
3. **B3:** judge the Module 3 pack; rule on whether the missing Mission Planner screenshot and unopened SERIAL1 block the stamp; **provision a read-only viewing endpoint** (second TCP port per vehicle, or MAVProxy map as the known-good fallback) — commander ports 5760–5810 stay exclusive.
4. **Cowork:** re-run Module 3 with the viewing endpoint live; capture the ring screenshot that was missing from the pack.
**Week 1 done when:** B3 stamps Module 3, and Samrat has watched the ring form on a map in real time. *(Done — 12 Sep run: 6 vehicles, 5-interceptor ring at 5 m/72°, 2.21 s convergence, ~3 cm mean hold error, clean RTL/disarm. Full report on file.)*

### Week 2 (13 – 19 Sep) — Module 4 + honest numbers
**Goal: moving-target ring, measured under realistic conditions.**
1. **A12 re-baseline.** ***DESCOPED FROM STAGE 1 — see A24 (24 Sep 2026).*** Never executed at any fleet size despite being scheduled since 5 Sep; ruled out of Stage-1 scope on rubric-weight and schedule-risk grounds; deferred to P2 hardware validation.
2. **Module 4 (moving target):** not executed under degraded conditions for the same reason as item 1. The Sprint 3 3-interceptor run (§4a Week 3 item 2) validates the static-ring case at RTK-grade GPS; moving-target behaviour remains a P2/P3 hardware item.
3. **Learning loop:** deferred with items 1–2 — its first real mission needs the degraded-GPS baseline that A24 postpones to P2.
**Week 2 status: descoped for Stage 1 per A24, not "done."** Not a gate blocker for the UAV-X Stage 1 submission (Path C+, §4b) — the ring's Stage-1 role is the RTK-grade Sprint 3 evidence plus the A22 static-geometry claim, nothing more.

### Week 3 (20 – 26 Sep) — Module 5 and the P1 gate
**Goal: every failsafe proven, gate passed, fleet re-baselined at the finalized 3-interceptor size.**
1. **Module 5:** full state machine IDLE→SEARCH→TRACK→RING→RTL with each failsafe deliberately triggered — target-feed death (must HOLD, not chase), commander death (fleet must fail safe — this is also the behavioural proof of A7), battery, geofence, single-drone loss with the ring reforming.
2. **Fleet re-run (added by A20).** *(Done — 23 Sep, `SWARM_SPRINT3_VERIFICATION.md`: 3 interceptors, 120° spacing, `--count 4`, 2.20 s convergence, 4.5–5.9 cm mean hold error, min separation lower bound 5.84 m. PASS on flight behaviour and A9. Caveat: this run used default RTK-grade SITL GPS, not the A12 M10-degraded profile — valid for geometry/plumbing only, not for any accuracy claim in the paper. Three minor follow-ups logged in that report, none gate-blocking.)*
3. **Repo hardening:** README, `requirements.txt`, pinned deps, 4-vehicle launcher, install instructions verified from a clean shell. **UPDATE 25 Sep (A30): audited, NOT yet complete — see A30 for the four findings and the separate, more urgent git-commit issue found during that audit.**
4. **Master reviews the P1 gate** on B3's stamps for Modules 3, 4, 5. **Module 4/5's degraded-condition and moving-target requirements are satisfied for the Stage-1 submission gate by the A24 descope — the full P1 gate (as originally written) still applies before hardware ordering, per the P2 row in §4.**
**Week 3 done when:** P1 gate passes → interceptor #1 may be ordered.

**If Techfest eligibility comes back positive**, the UAV-X fork decision is made at the end of Week 1 — not before, and never at the cost of these three weeks. *(Resolved — UAV-X Stage 1, Path C+, adopted 17 Sep. See §4b.)*

## 4b. Path C+ — the UAV-X Stage 1 approach (adopted 17 Sep 2026)

**The decision in one sentence:** we keep the anti-drone containment mission, add a multi-hop relay layer *inside* it, and also implement UAV-X's own PoI survey mission on the same engine — so the submission completes their mission while the counter-UAS ring is presented as the demonstrated extension.

**Paths considered and rejected:**
- **Path A — submit the ring project unchanged.** Rejected: "mission completion" is 25% of the UAV-X rubric and is defined by *their* scenario (survey designated Points of Interest, prioritise emerging high-priority regions). A containment ring scores zero there however good the relay is, and it leaves us dependent on an organiser exemption whose exact wording is still unconfirmed.
- **Path B — fork a separate disaster-response project.** Rejected: ~2 weeks of throwaway code, pauses Modules 4–5 for longer, and duplicates infrastructure we already have.
- **Path C — ring plus relay only.** Superseded by C+ after council review, for the Path A reason above.

**Why the relay layer is ours, not a competition bolt-on.** Interceptors pursuing a target beyond the ground station's radio range lose their command link. The fix is drones passing MAVLink traffic through each other in a chain back to the GCS, rebuilt automatically when a drone drops out or moves out of range. This is a genuine gap in our own architecture that Techkriti would have forced us to solve anyway. UAV-X moved the date, not the requirement.

**Why the PoI survey mission is cheap.** "Fly to a waypoint, dwell, report, move to the next by priority" is *simpler* than ring geometry and runs on the existing commander, relay layer and scoring harness. Estimated 1–2 days on top of the relay work. It converts mission completion from a near-certain zero into scored points and removes the exemption dependency.

**Scoring map (UAV-X rubric → our system):**

| Criterion | Weight | What answers it |
|---|---|---|
| Mission completion | 25% | PoI survey mission: all points visited, priority-weighted ordering, within time |
| Communication resilience | 25% | Multi-hop relay chain; packet delivery ratio, latency, connectivity downtime |
| Autonomous relay & role management | 20% | Dynamic relay assignment and reassignment; role changes logged |
| Fault recovery & swarm reconfiguration | 15% | Deliberate UAV dropout and link degradation, chain rebuild, ring re-spacing |
| Safety & collision avoidance | 10% | Minimum inter-UAV separation, geofence, battery and GCS-loss failsafes |
| Innovation & technical merit | 5% | Counter-UAS containment as the demonstrated extension; evidence discipline |

**Reproducibility is a graded criterion and an existing strength.** The A9 evidence standard, logged telemetry, run-to-completion rule, and the separation of executor (Cowork) from judge (B3) are unusual for any team at this stage. This goes in the paper as its own section, not as a footnote.

**Timeline to 27 Sep:** relay layer complete and scored by 21 Sep; PoI survey mission on top of it; UI v1 by 23 Sep; demonstration run recorded 24 Sep; paper drafted in parallel from day one and finalised 26 Sep (Master's internal target, leaving 27 Sep as submission day with a one-day buffer — **the real external deadline is, and has always been, 27 Sep**; see A27, which fixes this wording after it caused confusion). **Modules 4–5 and the P1 gate resume 28 Sep.**

**STATUS FLAG (added 23 Sep, updated 25 Sep — see A23/A24/A25/A26/A27/A28/A29/A30):** the PoI survey + relay + both fault events have been executed and evidenced. **A26 (24 Sep, late evening) corrected the mission model to match the actual rulebook mission-constraints slide** — arena size/shape, base position, PoI count, PoI spawn timing, speed and several other constraints were wrong or unenforced in the earlier evidence. **A27 (24 Sep, late evening) selected seed 48 as demo/evidence seed, later withdrawn by A28 (25 Sep) after B3 found both of seed 48's A23 fault events landed on the same relay node** — a genuine methodology violation, not a wording issue. **Seed 201 is the current evidence-of-record (A28/A29)** — regenerated, independently re-verified by B3, no defects found. Demo video recorded against the seed-201 UI replay (A29 item 1, confirmed 25 Sep — see A29). Repo hardening audited but **not yet complete** (A30). Paper (B4) cleared to finalize against seed 201 and the corrected 1000-seed aggregates (A29). Remaining critical-path items, in order: **close out A30's four repo-hardening gaps**, confirm the git-commit/push issue A30 also surfaced is resolved, paper finalization (internal target 26 Sep; real deadline 27 Sep).

**Fleet size for the relay/survey simulation (added by A21):** `uavx/` runs at N_UAV=4 — the 3 interceptors plus the Eye, which is relay-eligible on the same footing as the interceptors. See A21.

**Containment claim in the paper (added by A22):** the "ring surrounds the target" claim holds at 3 interceptors (static geometry). The "no exploitable gap against an actively evading target" claim is quantifiably weaker at 3 than at 5 and must be stated with the v_interceptor/v_target ≥ π/N relationship in the limitations/scaling section, not asserted flatly. See A22.

**PoI set and fault-event methodology (added by A23):** see A23 for the ratified default PoI layout and the two-event fault methodology. **Superseded 24 Sep, twice:** first by the `GreedyChainAssigner` reachability-spread fix (coordinates regenerated, structural requirements unchanged). **Then, later the same night, by A26** — the PoI count (6→10), spawn behaviour (static→random position AND time), arena size/shape, and base position in A23's ruling are all superseded by the rulebook's actual mission-constraints slide. A23's fault-event *methodology* (report-gated selection, two separate events, never pooled, and — clarified by A28 — landing on genuinely *different relay nodes* as well as being well-separated in time) is unaffected and remains in force — **reaffirmed by A27, then sharpened by A28** after seed 48 was found to violate the node-distinctness reading of "different."

**A12/ring-accuracy scope (added by A24):** quantitative ring accuracy under degraded GPS/wind is out of Stage-1 scope, deferred to P2 hardware validation. See A24.

**Tick-ordering ratification (added by A25):** the fault-injection tick-lag design is formally ratified. See A25. **Seed-1 freeze superseded by A27 — the demo/evidence seed is now 48. Superseded again by A28/A29 — the demo/evidence seed is now 201.**

**Demo seed and fault-trigger mechanism (added by A27):** ~~demo/evidence seed is **48**~~ **SUPERSEDED by A28/A29 — see below.** A23's report-gated fault-trigger mechanism stays unchanged. See A27.

## 5. Amendment Log (rulings since SWARM_MASTER v1)

- **A1 — Frames are carbon.** TPU full frames rejected on weight (50–60 g vs ~30 g) and flex (tuning problems). TPU allowed only for non-structural printed parts (GPS mast, antenna mounts, camera mount). *Supersedes the TPU frame wording in SWARM_MASTER §2.*
- **A2 — Cowork executes simulation sessions.** B3 plans and judges; Cowork runs code against SITL. (This file, §2–3.)
- **A3 — CLOSED by A20 (see below).** Was OPEN: real total ≈ ₹1,26,000 vs ₹99,000 snapshot. Options had been: (a) fund the gap, (b) 4 interceptors (square ring, saves ~₹22k), (c) cheaper FC/motors with quality risk. A20 resolved this by headcount, close in spirit to option (b) but landing on 3 interceptors + 1 Eye rather than 4 interceptors alone.
- **A4 — End-state definition and phase gates.** (§1, §4 of this file.)
- **A5 — Cowork is the single executor for the whole project.** Thinking chats think; Cowork does. In time-boxed sprints, Master may hand Cowork a self-contained sprint prompt directly, provided every safety rule in CLAUDE.md travels inside the prompt and B3 still judges the evidence afterward. (§2–3 of this file.)
- **A6 — Six SITL vehicles ratified, SIM ONLY.** In simulation, SYSID 6 is the **simulated THREAT** — a stand-in target that hovers/moves so the ring has something real to track. It is NOT the Eye, it never joins the formation, and it never receives ring setpoints. Resolves B3's scope flag of 2 Aug; the sprint's `--count 6` was Master-authorized. **The "5 interceptors" fleet-size clause this ruling assumed is superseded by A20** — the vehicle-count convention (target as the highest SYSID, threat never in the ring) still holds, just at `--count 4` (3 interceptors + target) for interceptor-only SITL work, or the values A21 sets for the relay/survey simulation.
- **A7 — SYSID_MYGCS pinned to the commander (250) on every vehicle, SITL and hardware.** RATIFIED. The GCS-loss failsafe must watch the entity actually commanding the fleet, not whichever Mission Planner client happens to attach. With this set: MP disconnecting is harmless; the commander dying still fails the fleet safe — exactly the intended design in CLAUDE.md. Mission Planner remains a read-only viewer on SERIAL1 ports.
- **A8 — DRIFT_RTL_REQUIRE_CONVERGED = True.** RATIFIED with one condition: the not-yet-converged phase must carry its own timeout — a drone that cannot reach its slot within the configured limit goes to LAND/RTL and is reported. No drone may loiter unconverged indefinitely. Any future change to either value returns to Master.
- **A9 — Evidence standard (restated as a ruling).** No module passes without B3's pre-specified "done when" artifacts: the JSON log, the run summary, and verbatim STATUSTEXT. A build log ending in Ctrl-C is not evidence. Cowork sessions must run to completion — through RTL and disarm — before the terminal is touched.

- **A10 — Module 3 passes on flight behaviour; A7 is not a sim gate but IS a hardware gate.** The 5 Sep rerun met its mission goals (6 vehicles connected, ring formed at 5 m/72°, held 60 s, converged 2.21 s each, clean RTL and disarm at 211 s, A9 satisfied). `SYSID_MYGCS` readback failed 0/6. Ruling: a mis-attributed GCS failsafe cannot injure anyone in SITL, so A7 does **not** block Module 4 or 5 in simulation. A7 confirmed by independent readback is a **hard gate before P2** — no real drone flies without it. Formal module stamp remains B3's.
- **A11 — mavlink-router REJECTED pending root cause.** Cowork proposed adopting mavlink-router to fix the A7 echo and SERIAL1 exposure. Rejected for now on two grounds: (i) it is new infrastructure proposed for a bug nobody has root-caused — the evidence cannot yet distinguish "PARAM_VALUE echo lost" from "PARAM_SET never applied," and Cowork's report asserts the former without showing the side-effect it claims verifies it; (ii) the proposal routes Mission Planner over UDP 14550–14555, and WSL2 does not forward UDP — the exact failure already documented in SWARM_B3_SITL_RECOVERY.md. Diagnose first. If mavlink-router is later adopted, **TCP endpoints only.**
- **A12 — No baseline or published figure may come from a default-SITL run.** The 5 Sep run reported `FIX 6` (RTK Fixed, centimetre-class) and zero wind, producing mean ring errors of 4.6–6.8 cm. Real hardware is an M10-class module at roughly 1.5–2 m, outdoors, in wind. That run therefore measures the geometry and the plumbing, **not achievable accuracy.** Ruling: (i) the learning-loop baseline must come from a run with GPS degraded to M10 class and wind enabled; (ii) no sub-metre accuracy figure from default SITL may appear in any competition submission, proposal, or improvement graph; (iii) the 4–5 m ring spacing in SWARM_MASTER §3.1 stands. **UPDATE 24 Sep (A24): the degraded-GPS/wind re-baseline this ruling calls for is descoped from Stage 1 and deferred to P2 hardware validation — A12's underlying rule (no RTK-grade number cited as real accuracy) remains in force and is exactly why the ring's Stage-1 evidence is presented qualitatively, per A22/A24.**
- **A13 — Mission Planner is a read-only viewer on its own endpoint, never a commander port.** Commander owns TCP 5760/5770/5780/5790/5800/5810 exclusively. `ports.txt` (5 Sep) confirms SERIAL1 was never opened under `--no-mavproxy`, so a viewing endpoint must be provisioned before Mission Planner is attached to a live mission. Provisioning is B3's lane; MAVProxy's map module is the known-good fallback. Attaching MP to a commander port reproduces the Sprint 1 GCS-failsafe RTL.

- **A14 — The parameter is `MAV_GCS_SYSID`, not `SYSID_MYGCS`.** ArduCopter 4.8.0-dev renamed it; ArduPilot silently discards PARAM_SET for unknown names, with no ack and no error — which is the exact "0/6 vehicles ever echo" symptom seen across every prior sprint. Confirmed independently via a raw MAVProxy FTP parameter dump on a clean boot: `MAV_GCS_SYSID` present at default 255, `SYSID_MYGCS` absent from all ~1,387 parameters. A7's intent is unchanged; only the name was wrong. **A10's hardware gate is now SATISFIED** — all six vehicles confirmed `MAV_GCS_SYSID = 250` by readback across two independent connection cycles (12 Sep). This also retroactively explains the Sprint 1 failsafe RTL: at the default of 255, any attaching client could be treated as the commanding ground station.
- **A15 — Tooling split.** Claude Code writes repo code; Cowork runs SITL sessions and captures evidence. B2 writes the Claude Code prompts (Master does not write them directly — a lapse on 17 Sep, corrected). B3 plans and judges sessions.
- **A16 — The mission UI is READ-ONLY.** It never sends commands, never arms, never modifies vehicle state, and imports nothing from the commander's control path. A web surface that can command drones is a new path into arming and control logic; that is designed deliberately after Stage 1, never in a 10-day sprint.
- **A17 — UI v1 is ONE panel.** A top-down map showing drone positions and the target, with lines between drones representing the current relay chain, colour-coded by link quality, rooted at the ground station, updating live as the chain reconfigures. No status table, no camera panel, no styling effort. Rationale: the UI scores no points directly, but the demonstration video does, and chain reconfiguration is the single most valuable visible behaviour (45% of the rubric across communication resilience and relay management). A panel-registry pattern is required so later panels drop in without touching existing ones.
- **A18 — Detection is modelled for Stage 1, not built.** The commander ingests a target position with realistic noise injected. No cameras exist in hardware or in simulation. This is stated plainly in the paper as a modelled sensor with the airborne implementation as Phase 2 — standard research practice, not a concealment. Rejected again for this sprint: airborne cameras for triangulation (tilt error of 1–2° at 100 m produces metres of bearing error, and a sideways view puts the target against ground clutter instead of clean sky). No vision work before 28 Sep.

- **A19 — GCS-loss position-hold behavior: paper-only for Stage 1.** When a UAV loses every path back to the ground station — not just its own direct link, but every hop through the relay chain — the correct behavior is to hold position while the remaining relays reposition to try to restore a path, and only trigger ArduPilot's own GCS-loss failsafe (RTL) if the chain is not restored within a bounded timeout. The timeout, and the failsafe being pinned to the commander (`MAV_GCS_SYSID = 250`, A7/A14) rather than any attached client, are what stop a drone from either loitering unreachable indefinitely or RTL-ing the instant a single relay blinks. RULING: for UAV-X Stage 1 this behavior is specified and reasoned about in the paper (communication resilience / fault recovery sections) but is **not** implemented as live flight logic — `uavx/` is a pure-Python relay/survey model with no MAVLink path, so there is no vehicle to hold position in that simulation. Implementing it as real flight logic is Phase 2, gated the same way A18's camera work is. B2 flagged this as needing an explicit ruling because "the paper describes it, the code doesn't do it" reads as an unstated gap to a judge unless it's a stated one — it now is.
- **A20 — Fleet finalized: 3 interceptors + 1 Eye, 4 airframes total.** RATIFIED by Samrat, 22 Sep 2026 ("We have finalized the 3+1 fleet that is guaranteed, so that is done"). Supersedes the "5 interceptors" figures in SWARM_MASTER §2/§3, in A6's fleet-size assumption, and throughout this file's §1 and §4 (both updated above). Ring formation is now 3 interceptors at 120° spacing around the target; radius unchanged at 4–5 m (SWARM_MASTER §3.1). This closes A3: the budget gap is resolved by headcount reduction, in the spirit of A3's option (b) but landing on 3 interceptors + 1 Eye rather than 4 interceptors alone — B1 is unpaused (§2). The Eye remains, per A6, a camera/relay platform and not a ring member; it does not receive ring setpoints. **What this does and doesn't validate:** the 12 Sep Module 3 evidence (5 interceptors, 72° spacing, 2.21 s convergence, ~3 cm mean hold error) stands as historical proof that the ring algorithm generalizes across N — it is not, on its own, evidence for the now-current 3-interceptor configuration. The ring mission must be re-run at 3 interceptors / 120° spacing / `--count 4` (Sprint Plan §4a, Week 3, item 2) before any accuracy or convergence number is cited as the current fleet's performance in a report, paper, or demo claim. Any UAV-X stress-test evidence already collected at the old 5-interceptor N or without the Eye must likewise be re-run — see A21. **UPDATE 23 Sep: the ring re-run is now done — `SWARM_SPRINT3_VERIFICATION.md`, PASS. See §4a Week 3 item 2.**
- **A21 — The Eye is a relay-capable node.** Resolves B2's flagged ambiguity over whether N_UAV=4 in the UAV-X relay/survey simulation includes the Eye. RULING: yes — the Eye carries the same radio as the interceptors (SWARM_MASTER hardware ruling, unchanged) and its overhead position makes it, if anything, the best-placed node in the fleet for a relay role: nothing else in the air is more likely to hold line-of-sight to both the ground station and the other three airframes at once. There is no principled reason to model it as a mute passenger. All UAV-X relay/survey simulation and stress-test work (`uavx/`) therefore runs at N_UAV=4 going forward, with the Eye eligible for relay assignment on the same footing as the three interceptors. Any stress-test evidence already collected at N_UAV=5 (the old 5-interceptor fleet) or at N_UAV=4-without-Eye-eligibility must be re-run at the correct configuration before being cited in the paper — already flagged to B2 as required, restated here as a formal ruling.
- **A22 — 120° spacing (3 interceptors) is geometrically a ring; the dynamic containment guarantee against an evading target is quantifiably weaker than at 5, and paper language must reflect it.** Two distinct claims hide under "ring": (1) static geometry — the target sits inside the interceptors' convex hull, true for any N≥3, unaffected by the fleet cut; a triangle encloses a point exactly as a pentagon does. (2) dynamic containment — no gap an evading target can exploit. Modelling the worst case (target breaks straight through the angular midpoint of the largest inter-interceptor gap, interceptor must close half that gap's arc, at the ring radius, no slower than the target closes the same radius) gives: **v_interceptor / v_target ≥ θ_gap / 2 = π / N**. At N=5 (72° gap) the required ratio is ≈0.628 — interceptors can be slower than the target and still geometrically close the worst-case gap. At N=3 (120° gap) the required ratio is ≈1.047 — interceptors must now be *faster* than the target to guarantee the same closure. This is a real, computed tightening (matches the 5/3≈1.67× scaling visible in the raw angle ratio) — not a wording issue. RULING: (i) "ring" / "surrounds the target" stays in the paper and demo script for the static claim, unqualified. (ii) The paper must NOT claim guaranteed containment against an actively, adversarially evading target at N=3 without qualification; the v_i/v_t ≥ π/N relationship goes explicitly into the scaling/limitations section rather than being left for a judge to derive themselves. (iii) The demo itself needs no softening — the target flies a scripted or beacon-tracked path, not an adversarial worst-case breakout, so the visual "surrounds and tracks" claim holds as filmed. (iv) Flagged, not required for Stage 1: a "predictive re-slotting" behavior (interceptors biasing toward the gap nearest the target's current velocity vector) would partially close this margin and is worth one sentence as named future work if B4 wants it.
- **A23 — PoI set and fault-event methodology, ratified as defaults (23 Sep 2026).** B3 flagged these as entirely undecided, blocking the demo script, with 4 days left and no evidence any of the associated work (relay/survey execution, UI existence) has happened. RULING, to unblock immediately:
  - **PoI set:** 6 PoIs, two priority tiers — 3 at priority=3, 3 at priority=1 (3× separation so `score = priority/distance` produces a visibly non-arbitrary ordering). Layout requirement: at least 2 PoIs reachable in a single hop from GCS, at least 2 requiring exactly one relay hop, at least 1 requiring two hops — the mission must exercise multi-hop, not just relay-exists. Exact coordinates are B2's to set, using the real link-range/PDR constant already defined in `uavx/config.py` (Master does not have that number and will not guess it).
  - **Fault-event methodology:** two separate deliberate events, not one, well separated in time. Event 1 (link degradation, not full dropout) at ≈40% of mission time, applied to whichever relay node is *verifiably carrying live PoI-report traffic* at that instant — chosen programmatically at runtime, never a hardcoded sysid. Event 2 (full UAV dropout) at ≈75% of mission time, applied to whichever *different* relay node is live on the chain at that instant. Both nodes and exact trigger timestamps must be logged. `recovery_time_s` and `relay_reallocations` are reported separately per event, never pooled — this is the same "verifiably on the live chain" standard already required for the earlier N=4 stress-test rerun, applied consistently here so the same "was that node even live?" problem doesn't recur.
  - **Status, stated plainly:** as of this ruling, nothing in project knowledge shows `uavx/survey.py` has ever run, that either fault event has been executed, or that the A17 UI panel exists. The "21 Sep"/"23 Sep" dates in §4b's timeline appear to have passed without anyone producing evidence, which is the same unverified-"it's done" pattern already flagged once tonight for the stress test. This is now the critical path — see the STATUS FLAG in §4b. **UPDATE 24 Sep: this work is now done — see A24/A25 preamble and `claude/SWARM_STAGE1_RELAY_POI_PLAN.md`. PoI coordinates were regenerated against the real relay-placement code; the structural requirements above are unchanged.** **UPDATE 24 Sep, later: superseded again by A26 — the PoI count/layout/spawn-timing/arena assumptions in this ruling no longer hold. See A26.** **UPDATE 24 Sep, later still: the fault-event methodology in this ruling was directly challenged (low clean-separation rate under A26's dynamic model) and reaffirmed unchanged by A27 — see A27.** **UPDATE 25 Sep: A28 confirmed "different relay node" in this ruling means genuinely distinct node identity, not merely a freshly-drawn instant — see A28.**
- **A24 — Quantitative ring accuracy under degraded GPS/wind (A12) is out of scope for UAV-X Stage 1; deferred to P2 hardware validation.** RULING (24 Sep 2026), in response to B3 flagging that A12's re-baseline has never been run, at any fleet size, despite being scheduled since 5 Sep. Reasons: (i) **Rubric weight.** The ring feeds only Innovation (5%) and part of Safety (10%) — a combined ≤15% of the UAV-X rubric — versus the 85% carried by mission completion, communication resilience, relay management, and fault recovery (A23's work), which had zero verified evidence as of 23 Sep and needed the remaining time. (ii) **Novel-configuration risk.** A12 at 3 interceptors + wind + degraded GPS together has never been attempted once, at any N — every previous first-time SITL configuration in this project (A7/A14's parameter-name mystery, the viewer-port collision, the stale-file glob bug) cost hours to days of debugging, not the nominal ~30 minutes a clean run would take, and that risk is not worth taking against the low-value side of the rubric with 2 working days left. (iii) **A22 already supplies a defensible Stage-1 claim without a hard number** — static-geometry containment plus the Sprint 3 RTK-grade plumbing evidence (both explicitly labelled per A12's original caveat), same honest-deferral pattern as A18's camera detection. **What this does NOT rule:** A12 is not cancelled — it is deferred to P2, run against real hardware GPS instead of simulated M10 degradation, where it belongs anyway (§4, P2 row). If all other Stage-1 critical-path items (UI confirmation, demo video, paper) are in a safe state before 26 Sep, B3 may attempt a hard-capped 30-minute A12 stretch run, abandoned immediately on any first-try failure, treated as a bonus and never a dependency.
- **A25 — Fault-injection tick-ordering, ratified.** RULING (24 Sep 2026), closing item 1 of `claude/SWARM_STAGE1_RELAY_POI_PLAN.md`'s open items. B3's investigation found no prior decision record existed for this despite Claude Code having cited one — the same "confident assertion standing in for a real decision" pattern already caught once in this project (B2's inverted A21 rendering). Ratifying B3's recommendation, since B2 has not disputed it and time does not allow further ping-pong: fault application is tied to the 1-tick (0.2s) lag after an observable event (report-success), not an arbitrary clock tick — defensible and easier to explain to a judge than a bare timer. The known cost is a 2/300-seed race window, already isolated: seed 1 (used for the evidence pack) is verified clean on both fault events, and is **frozen for the demo** — do not switch seeds casually before 27 Sep. **UPDATE 24 Sep, later: the seed-1 freeze is superseded by A27 — A26's dynamic PoI model changed the event stream, and seed 1 no longer produces two separated fault events under it. Demo/evidence seed is now 48. UPDATE 25 Sep: superseded again — demo/evidence seed is now 201, see A28/A29.**
- **A26 — Mission model corrected to match the actual UAV-X rulebook mission-constraints slide (24 Sep 2026, late evening).** RULING, triggered by Samrat producing a photo of the rulebook's own "Mission constraints" slide (Ministry of Electronics & IT / IIT Kanpur letterhead), which every prior `uavx/config.py` PROVISIONAL guess in this area turned out to conflict with. This supersedes A23's PoI-count/layout/arena clauses (fault-event *methodology* is unaffected — see A23's update note). Confirmed REAL (no longer guesses): comm range 100 m (already correct, was guessed for unrelated reasons), operational area 1000×1000 m, 10 PoIs (was 6), PoIs spawn at **random positions AND times during the mission** (was: 6 static PoIs all known at t=0), max speed 5 m/s (was 12 m/s), UAV flight time 20 min (already correct, was guessed), mission duration 45 min, min inter-vehicle separation 20 m (not previously modelled), max altitude 100 m (not previously modelled, sim stays 2D — see below), max 10 s from PoI detection to report reaching the GCS (not previously modelled; the old DWELL_S=15s/REPORT_TIMEOUT_S=20s pair is now impossible to satisfy and was NOT reused). Base sits 75 m **outside** the operational area (not at its center, as every prior scenario had it).
  - **Reachability finding, checked against link.py's real BFS, not eyeballed:** with the settled 4-aircraft fleet (A20/A21 — 3 usable relays) and 100 m comm range, the longest possible relay chain reaches 4×100=400 m from base. Base sits 575 m from the arena's center and up to ~1186 m from its far corner. Only **~19% of the 1000×1000 m arena is physically reachable by any relay chain, regardless of assignment strategy** — not a bug, a consequence of the rulebook's own numbers. RULING (Samrat, presented with the alternative of restricting PoIs to the reachable zone): PoIs are spread across the **whole** arena per the rulebook, not confined to the reachable pocket. Most will be detected but never reported in time — this is deliberately kept and reported honestly as the resilience story ("Resilient BVLOS Swarm Challenge"), not hidden or engineered around. Confirmed by a multi-seed smoke test of the new `uavx/survey.py::run_dynamic()`: typically 1-2 of 10 PoIs successfully report per run, the rest are visited but timed out — consistent with the ~19% reachability figure. **UPDATE 24 Sep, later (A27): B3's 100-seed scan of this model confirms the smoke test — ~2/10 PoIs physically reachable, ~1.1/10 reported on average across 100 seeds. This is the expected, already-ruled-on consequence of this paragraph, not a new finding requiring separate action.**
  - **New mechanic, not a parameter tweak:** dynamic PoI spawning required a genuinely new event-driven mission loop (`run_dynamic()`, kept separate from `run()` rather than merged into it, so the CP1-CP5 evidence already verified tonight against the R_COMM_M 150→100 migration is not put at risk). Spawn-time distribution (uniform across the 45-min window) and the dwell/report-deadline split (3 s dwell + 7 s reporting margin inside the 10 s budget) are PROVISIONAL — the rulebook gives the constraints, not these distributions — documented in `uavx/config.py`/`uavx/RULES_NOTES.md` section 14 rather than silently assumed.
  - **Known gaps, explicitly deferred, not built tonight:** (i) altitude/3D modelling — sim stays 2D, all aircraft treated as flying at a common altitude band for comm-range purposes (consistent with the existing no-altitude limitation already logged in RULES_NOTES.md section 11); (ii) 20 m minimum-separation enforcement; (iii) 20-minute battery/flight-time depletion enforcement (the number was already correct by coincidence, only its enforcement is new and still missing); (iv) the "all" fault-event set (uav_fail/uav_recharge) is not supported under dynamic spawning, only A23's link_degraded/uav_dropout pair; (v) `evidence_pack.py` and the CP1-CP5 evidence pack itself have NOT yet been regenerated against this corrected model — tonight's already-published R_COMM_M=100 evidence pack is now stale and must not be cited as the Stage-1 submission's final numbers until this is done. **This is now the single most critical open item — see the STATUS FLAG in §4b.**
- **A27 — Demo seed switched to 48; A23's fault-trigger mechanism reaffirmed unchanged; Stage-1 deadline wording clarified (24 Sep 2026, late evening).** RULING, closing B3's post-A26 100-seed scan follow-up (three items brought to Master together).
  - **Deadline — no real conflict, wording fixed.** The rulebook's hard deadline — confirmed against the rulebook PDF and restated in the draft's own submission checklist (`claude/UAV-X_Stage1_draft.md` §"Stage 1 Submission Checklist") — is **27 September 2026, by email only, to pushpak_gc2026@aero.iitb.ac.in.** "Finalised 26 Sep" in §4b's Path C+ timeline and the STATUS FLAG was always Master's internal target, leaving 27 Sep as the actual submission day with a one-day buffer for final checks and sending the email — not a second, competing deadline. Both figures were correct; the doc simply never said so in one place, which is exactly the kind of ambiguity that costs time two days out. Fixed inline in §4b above. **The real external deadline is, and has always been, 27 September.**
  - **Demo/evidence seed: switched from 1 (A25) to 48.** A25 froze seed 1 under the pre-A26 mission model. A26's dynamic PoI spawning (random position and time) changed the underlying event stream enough that seed 1 no longer produces two separated fault events under the corrected model. B3's 100-seed scan of the corrected model found only seed 48 satisfies A23's fault-event methodology in full — both events land on different, verifiably-live relay nodes, dropout recovers in 8.0 s. RULING: approved, **on condition** the paper and evidence pack cite seed 48 alongside the 100-seed aggregate statistics (reachability ~2/10, mean PoI-report rate ~1.1/10, clean fault-separation rate 6/100) — a single seed presented with no context is a cherry-pick; the same seed presented next to its distribution, with the mechanism below explained, is a representative case honestly labelled. This is a *stronger* story for a "resilience" scenario than hiding the distribution would be. Supersedes A25's seed-1 freeze. **UPDATE 25 Sep: WITHDRAWN by A28 — the "different, verifiably-live relay nodes" claim above does not hold for seed 48 (both events verified to hit relay_2). Do not cite seed 48. See A28.**
  - **Fault-trigger mechanism: A23's report-gated design stays. NOT changed to packet-traffic-gating.** B3 asked whether to switch the trigger from "verifiably carrying live report traffic" (A23) to "live packet traffic," which would fire reliably and separately on far more seeds. RULING: no, keep A23 as-is. Reasoning: (i) the 6/100 clean-separation rate is not evidence the trigger design is broken — it is the direct, mechanical consequence of A26's own finding that only ~1-2/10 PoIs report per run under the rulebook's real comm-range/arena numbers. Fewer report events means fewer live-traffic ticks for the two fault windows (~40%/~75% mission time) to land on distinct nodes, so they collide more often than they would in a richer event stream — an honest property of a harsh mission, not a defect in the trigger. (ii) A23's report-gated standard is the same evidentiary bar already applied to the earlier N=4 stress-test rerun; switching to packet-traffic-gating for reliability trades that consistency for cosmetic convenience. (iii) Switching would require regenerating the entire evidence pack a second time, on top of the A26 regeneration already flagged as the single most critical open item, with ~2-3 days left — and a fully A23-compliant example (seed 48) already exists under the current honest mechanism, so there is nothing left to buy by changing it. Same risk-vs-reward logic as A24. **If time allows after every other critical-path item is done** (A26 regeneration, demo video, repo hardening, paper), B3 may investigate packet-traffic-gating as a documented "future work" item for Stage 2 — never as a Stage-1 dependency. **UPDATE 25 Sep: the 6/100 figure cited above was also found miscounted — recomputed at 7/100 from the same 100-seed sample, then superseded entirely by a wider 1000-seed rescan under A28. See A28/A29 for current figures.**
- **A28 — Seed 48 withdrawn; node-distinctness confirmed as A23's real requirement; 1000-seed rescan authorized (25 Sep 2026).** RULING, closing B3's evidence-pack verification finding. B3 found two problems in A27's justification for seed 48: (1) A27 claimed seed 48's two fault events land on "different, verifiably-live relay nodes" — they don't; `event_metrics.json` shows both `link_degraded` and `uav_dropout` firing on the same node, `relay_2`. (2) the cited "6/100" clean-separation rate was miscounted; the correct figure from the same 100-seed sample is 7/100. RULING: (1) B3 read A23 correctly — "different" in A23's original wording means the two fault events must land on genuinely distinct relay nodes, not merely be freshly/independently drawn. Seed 48 fails this. Its approval in A27 is **withdrawn**; do not cite seed 48 anywhere in the paper, evidence pack, or demo materials going forward. (2) 6/100 → 7/100 confirmed as a fix, but flagged as provisional — it doesn't filter for node-distinctness, so it will likely change once the node-aware rescan below runs. (3) **Authorized:** rescan seeds with a node-ID field added, out to at least 1,000 seeds (pure-Python, no SITL — cheap to run wide) to find a seed satisfying all three conditions: both fault events fire, both/the recoverable one recovers, and the two land on genuinely distinct nodes. (4) If 1,000 seeds still turns up nothing: stop immediately and escalate back to Master rather than reinterpreting "different" to force a pass — a structural finding (too few live nodes to choose between, given A26's small reachable pocket) would need its own ruling, not a workaround.
- **A29 — Seed 201 adopted as evidence-of-record; critical-path sequencing set (25 Sep 2026).** RULING, following B3/Cowork's 1000-seed rescan under A28. Seed 201 is the evidence-of-record going forward — B3 selected it as the lowest-numbered seed satisfying node-distinctness (A28), time-separation, and report-distinctness together (seed 4, the lowest satisfying node-distinctness alone, was checked and rejected for firing both events at the identical tick off the identical report — the exact defect A23 exists to prevent, via a different mechanism). Sequencing, in order: (1) record the demo video off the UI replaying the seed-201 pack — **done, confirmed 25 Sep.** (2) confirm repo hardening (§4a Week 3 item 3) is actually done, not just planned — **audited 25 Sep, found NOT complete, see A30.** (3) B4 cleared to finalize the paper against seed 201 and the 1000-seed aggregates: reachability ~1.97/10, reported ~1.07/10, fully A23+A28-compliant seeds 18/1000 (the stricter figure, also separated in time/report — use this one for citation, not the looser 33/1000 or 84/1000 intermediate-filter numbers).
- **A30 — Repo hardening audit findings; git-commit/push emergency surfaced during the audit (25 Sep 2026).** B3-requested audit of Sprint Plan §4a Week 3 item 3, executed by Cowork, reported here as a status log entry (not a new ruling requiring interpretation — Master ratifies its inclusion after the fact). **Repo hardening — NOT yet complete:** (1) `requirements.txt` missing entirely. (2) README's install steps (run/score/view commands) verified working, but omit a `git clone` step. (3) The documented run command verified working end-to-end from a genuinely fresh venv. (4) The 4-vehicle launcher script itself is current and correct (matches A20/A21's N_UAV=4 fleet), but the accompanying checklist text still says "6-vehicle" — stale, needs updating. **Separate and more urgent — surfaced during the same audit:** the repository (`/home/satis/techkriti-swarm/techkriti-swarm/`, remote `github.com/SamratSamratSamrat/techkriti-swarm.git`) has exactly one git commit ever, the initial commit (24 Sep 13:17:32). Every ruling and code change since then — A14 through this entry, including the entire seed-48-to-201 fix — existed only as **uncommitted working-tree changes** until this was caught. **Separately, this same audit found this file itself had silently diverged into three copies**: two on-disk copies (this repo and a second, unrelated `/home/satis/swarm-defence/` clone) both capping at A18, while the Project-knowledge mirror synced into this Claude Project held the real, already-ratified A19 through A27 — meaning rulings made in real time were never actually committed to the working repository. This file has been reconciled against the Project-knowledge copy (the authoritative source) as of this entry, with A28–A30 appended. **Immediate required actions, before anything else:** (i) commit and push everything now, so the last two days of work exists somewhere beyond one laptop; (ii) close A30's four repo-hardening gaps above; (iii) going forward, this file is edited only by Master/Samrat per the file header — Cowork reports findings for the owning chat to log, it does not append ruling entries itself (this rule already existed in §2's Cowork charter and is restated there, bolded, as of this entry, because it was the proximate cause of the A19-numbering collision that surfaced this whole issue).

*End of operations document. Last amended 25 September 2026 (A30: repo-hardening audit findings logged; the file itself reconciled after being found to have silently diverged from the real, ratified A19-A27 for over 24 hours; a critical uncommitted-git-state issue surfaced and flagged for immediate action ahead of the 27 Sep submission).*
